const initialState = {
    files: []
}

const filesReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export {
    filesReducer
}
